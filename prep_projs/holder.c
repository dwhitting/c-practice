    #include "wrap_prox.h"
    
    // /* walk results list and display each ip */
    // int flags = NI_NUMERICHOST;     /* display addr string instandn of domain name */
    // char buf[MAXLINE];
    // for (ind_res = results; ind_res; ind_res = ind_res->ai_next) {
    //     getnameinfo(ind_res->ai_addr, ind_res->ai_addrlen, buf, MAXLINE, NULL, 0, flags);
    //     printf("%s\n", buf);
    // }